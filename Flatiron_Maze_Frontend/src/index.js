let currentUser = {}

// fetch(URL/${id})
// 	.then(resp => resp.json())
// 	.then(data => (data) => {
// 		currentUser = data
// 	})
// 	.catch()

currentUser = {
	id: 1,
	name: "Player1"
}

function login() {
	// Is currentuser blank? If so, login screen. Render home page after
}